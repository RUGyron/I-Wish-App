# IWish v1.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Один билд v1.1 со всеми накопленными правками + миграция existing зашифрованных данных без потерь + Force Update инфра + iPad адаптация.

**Architecture:** Additive-only schema migration + preserve-unknown-keys паттерн в updateXxx + one-time backfill при первом старте v1.1. SwiftUI + SwiftData + Firebase Firestore (REST) + Firebase Remote Config. iPad через NavigationSplitView + adaptive grid. Force update — blocking screen на старте по min_supported_build из RC.

**Tech Stack:** Swift 5.9+, SwiftUI, SwiftData, Firebase Auth, Firebase Firestore (custom REST client), Firebase RemoteConfig (новое), AES-GCM-256 (CryptoKit).

**Spec:** `docs/superpowers/specs/2026-05-08-iwish-v11-design.md`

---

## File Structure

**Существующие файлы (изменения):**
- `IWish/Models/Item.swift` — +`priceMaxValue: Double?`
- `IWish/Models/Wishlist.swift` — +`gradientHue: Double?`
- `IWish/Services/AuthService.swift` — name gate (без `?? "Пользователь"`) + `requiresNameRecovery`
- `IWish/Services/DataService.swift` — preserve unknown в updateItem + новые параметры (description, coverImageData, linkMetadataData, probationEndAt, priceMax) + backfill helper
- `IWish/Services/FirestoreService.swift` — preserve unknown в update*Item/Wishlist + новые ключи payload + memberships userName + cap до 300
- `IWish/Services/InputLimits.swift` — `maxFirestoreRequestsPerMinute = 300`
- `IWish/Services/DefaultCoverGenerator.swift` — +`colors(forHue:)` + `colors(for wishlist:)`
- `IWish/Views/AddItemSheet.swift` — keyboard, range pricing UI, parsePrice Int
- `IWish/Views/EditItemSheet.swift` — keyboard, range pricing UI, decimal display fix, Int parsing
- `IWish/Views/AddWishlistSheet.swift` — keyboard, hue picker integration
- `IWish/Views/EditWishlistSheet.swift` — keyboard, hue picker integration
- `IWish/Views/WishlistDetailView.swift` — tier menu emoji always, card radius 8, range price display, sum range
- `IWish/Views/HomeView.swift` — safe area fix, iPad grid columns
- `IWish/Views/SettingsView.swift` — version без билда
- `IWish/Views/ParticipantsView.swift` — member.userName из memberships
- `IWish/Views/JoinWishlistSheet.swift` — joinWishlist передаёт userName
- `IWish/Views/RootView.swift` — split view + force update gate + name gate
- `IWish/Views/ItemDetailSheet.swift` — range "X — Y" display
- `IWish/Views/Components/WishlistTileView.swift` — gradientHue rendering через `colors(for:)`
- `IWish/IWishApp.swift` — runMigrationsIfNeeded + RemoteConfigService init
- `IWish.xcodeproj/project.pbxproj` — TARGETED_DEVICE_FAMILY="1,2", iPad orientations, version 1.1, build 5

**Новые файлы:**
- `IWish/Views/Components/GradientHuePicker.swift`
- `IWish/Views/NameRecoveryView.swift`
- `IWish/Views/ForceUpdateBlockingView.swift`
- `IWish/Services/RemoteConfigService.swift`

**Tests (новые):**
- `IWishTests/EncryptionRoundtripTests.swift`
- `IWishTests/ItemPriceModeTests.swift`

---

## Git policy для этого плана

После каждой задачи делаем commit + push (CLAUDE.md проекта: "коммитить и пушить сразу"). Сообщения чистые, без AI-меток.

После полного билда + manual test — подъём version в pbxproj и submit к ASC.

---

## Task 1: SwiftData additive migration — `Item.priceMaxValue` + `Wishlist.gradientHue`

**Files:**
- Modify: `IWish/Models/Item.swift`
- Modify: `IWish/Models/Wishlist.swift`

- [ ] **Step 1: Add `priceMaxValue` to Item**

В `IWish/Models/Item.swift`, после `var priceValue: Double?` добавить:

```swift
var priceValue: Double?
var priceMaxValue: Double?  // новое — если задан, item имеет диапазон min..max
var currency: String = "RUB"
```

Также обновить `init`:

```swift
init(
    name: String,
    tier: ItemTier = .maybe,
    sortIndex: Double = 1000.0,
    currency: String = "RUB",
    price: Double? = nil,
    priceMax: Double? = nil,
    descriptionText: String? = nil,
    url: String? = nil,
    coverImageData: Data? = nil,
    coverEmoji: String? = nil,
    addedByUID: String? = nil,
    addedByName: String? = nil
) {
    let now = Date.now
    self.id = UUID()
    self.name = name
    self.descriptionText = descriptionText
    self.coverImageData = coverImageData
    self.coverEmoji = coverEmoji
    self.priceValue = price
    self.priceMaxValue = priceMax
    self.currency = currency
    // ... остальное без изменений
}
```

И добавить computed property `priceMode`:

```swift
enum PriceMode {
    case none
    case exact(Double)
    case range(min: Double, max: Double)
}

var priceMode: PriceMode {
    switch (priceValue, priceMaxValue) {
    case (nil, nil): return .none
    case (let p?, nil): return .exact(p)
    case (let p?, let m?) where m > p: return .range(min: p, max: m)
    case (let p?, let m?): return .range(min: m, max: p)  // защита от перепутанных min/max
    case (nil, let m?): return .range(min: 0, max: m)
    }
}
```

- [ ] **Step 2: Add `gradientHue` to Wishlist**

В `IWish/Models/Wishlist.swift`, после `var gradientSeed: Int = 0` добавить:

```swift
var gradientSeed: Int = 0
var gradientHue: Double?  // 0...1 — если задан, используется для генерации; иначе fallback на gradientSeed
```

Обновить `init` — добавить параметр `gradientHue: Double? = nil`:

```swift
init(
    name: String,
    coverImageData: Data? = nil,
    coverEmoji: String? = nil,
    ownerRecordID: String? = nil,
    isShared: Bool = false,
    isArchived: Bool = false,
    sharedWishlistID: String? = nil,
    gradientSeed: Int? = nil,
    gradientHue: Double? = nil
) {
    // ... существующее
    self.gradientHue = gradientHue
}
```

- [ ] **Step 3: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs"
```

Expected: SUCCESS. SwiftData lightweight migration не требует ручных шагов для Optional fields.

- [ ] **Step 4: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Models/Item.swift IWish/Models/Wishlist.swift && git commit -m "model: add priceMaxValue and gradientHue optional fields for v1.1"
```

И push:

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git push
```

---

## Task 2: FirestoreService — preserve-unknown-keys в `updateSharedItem`

**Files:**
- Modify: `IWish/Services/FirestoreService.swift:515-535`

- [ ] **Step 1: Replace `updateSharedItem` body**

В `IWish/Services/FirestoreService.swift`, найти функцию `updateSharedItem(...)` и заменить body:

```swift
/// Update shared item с preserve-unknown-keys: GET → decrypt → merge → re-encrypt.
/// Это защищает forward-compat: если будущие версии добавят новые ключи в payload,
/// наш клиент сохранит их при write.
func updateSharedItem(
    wishlistID: String,
    itemID: String,
    name: String,
    tier: String,
    price: Double?,
    priceMax: Double?,
    currency: String,
    url: String?,
    emoji: String?,
    sortIndex: Double,
    isArchived: Bool,
    descriptionText: String?,
    coverImageData: Data?,
    linkMetadataData: Data?,
    probationEndAt: Date?,
    addedByUID: String? = nil,
    addedByName: String? = nil,
    key: SymmetricKey
) async throws {
    // 1. Pre-fetch existing payload, чтобы preserve unknown keys
    var existingPayload: [String: Any] = [:]
    if let existingDoc = try? await request("GET", path: "shared_wishlists/\(wishlistID)/items/\(itemID)"),
       let fields = existingDoc["fields"] as? [String: Any],
       let encryptedData = parseFields(fields)["encryptedPayload"] as? Data,
       let decrypted = try? EncryptionService.decrypt(encryptedData, using: key) {
        existingPayload = decrypted
    }

    // 2. Apply наши изменения. nil-значения = explicit стирание (уважаем callsite).
    let updates: [String: Any?] = [
        "name": name,
        "tier": tier,
        "price": price,
        "priceMax": priceMax,
        "currency": currency,
        "url": url,
        "coverEmoji": emoji,
        "coverImageData": coverImageData,
        "linkMeta": linkMetadataData,
        "description": descriptionText,
        "probationEndAt": probationEndAt.map { ISO8601DateFormatter().string(from: $0) },
        "addedByUID": addedByUID,
        "addedByName": addedByName
    ]

    var merged = existingPayload
    for (k, v) in updates {
        if let v {
            if let d = v as? Data {
                merged[k] = d.base64EncodedString()
            } else {
                merged[k] = v
            }
        } else {
            merged.removeValue(forKey: k)
        }
    }

    let encrypted = try EncryptionService.encrypt(merged, using: key)
    let fields = toFields([
        "encryptedPayload": encrypted as Any,
        "sortIndex": sortIndex as Any,
        "isArchived": isArchived as Any,
        "updatedAt": Date() as Any
    ])
    let _ = try await request("PATCH", path: "shared_wishlists/\(wishlistID)/items/\(itemID)", body: ["fields": fields])
}
```

- [ ] **Step 2: Update call sites in DataService**

Это будет в Task 5 (DataService новые параметры). Сейчас компиляция упадёт — это OK, исправим в следующем шаге.

Также удалить FIXME-комментарий перед updateSharedItem (строки 510-514) — больше не актуален.

- [ ] **Step 3: Apply same pattern в `updatePersonalItem`**

В `IWish/Services/FirestoreService.swift`, найти функцию `updatePersonalItem(...)` и заменить body аналогично:

```swift
func updatePersonalItem(
    uid: String,
    wishlistID: String,
    itemID: String,
    name: String,
    tier: String,
    price: Double?,
    priceMax: Double?,
    currency: String,
    url: String?,
    emoji: String?,
    sortIndex: Double,
    isArchived: Bool,
    descriptionText: String?,
    coverImageData: Data?,
    linkMetadataData: Data?,
    probationEndAt: Date?,
    addedByUID: String? = nil,
    addedByName: String? = nil,
    key: SymmetricKey
) async throws {
    var existingPayload: [String: Any] = [:]
    if let existingDoc = try? await request("GET", path: "users/\(uid)/wishlists/\(wishlistID)/items/\(itemID)"),
       let fields = existingDoc["fields"] as? [String: Any],
       let encryptedData = parseFields(fields)["encryptedPayload"] as? Data,
       let decrypted = try? EncryptionService.decrypt(encryptedData, using: key) {
        existingPayload = decrypted
    }

    let updates: [String: Any?] = [
        "name": name,
        "tier": tier,
        "price": price,
        "priceMax": priceMax,
        "currency": currency,
        "url": url,
        "coverEmoji": emoji,
        "coverImageData": coverImageData,
        "linkMeta": linkMetadataData,
        "description": descriptionText,
        "probationEndAt": probationEndAt.map { ISO8601DateFormatter().string(from: $0) },
        "addedByUID": addedByUID,
        "addedByName": addedByName
    ]

    var merged = existingPayload
    for (k, v) in updates {
        if let v {
            if let d = v as? Data {
                merged[k] = d.base64EncodedString()
            } else {
                merged[k] = v
            }
        } else {
            merged.removeValue(forKey: k)
        }
    }

    let encrypted = try EncryptionService.encrypt(merged, using: key)
    let fields = toFields([
        "encryptedPayload": encrypted as Any,
        "sortIndex": sortIndex as Any,
        "isArchived": isArchived as Any,
        "updatedAt": Date() as Any
    ])
    let _ = try await request("PATCH", path: "users/\(uid)/wishlists/\(wishlistID)/items/\(itemID)", body: ["fields": fields])
}
```

Удалить FIXME перед updatePersonalItem (строки 357-360).

- [ ] **Step 4: Update `createSharedItem` и `createPersonalItem` — добавить новые параметры**

`createSharedItem` — добавить новые параметры с default nil чтобы не ломать вызовы:

```swift
func createSharedItem(
    wishlistID: String,
    itemID: String,
    name: String,
    tier: String,
    price: Double?,
    priceMax: Double? = nil,
    currency: String,
    url: String?,
    emoji: String?,
    sortIndex: Double,
    descriptionText: String? = nil,
    coverImageData: Data? = nil,
    linkMetadataData: Data? = nil,
    probationEndAt: Date? = nil,
    addedByUID: String? = nil,
    addedByName: String? = nil,
    key: SymmetricKey
) async throws {
    let payload = EncryptionService.packPayload([
        "name": name,
        "tier": tier,
        "price": price,
        "priceMax": priceMax,
        "currency": currency,
        "url": url,
        "coverEmoji": emoji,
        "coverImageData": coverImageData,
        "linkMeta": linkMetadataData,
        "description": descriptionText,
        "probationEndAt": probationEndAt.map { ISO8601DateFormatter().string(from: $0) },
        "addedByUID": addedByUID,
        "addedByName": addedByName
    ])
    let encrypted = try EncryptionService.encrypt(payload, using: key)

    let fields = toFields([
        "encryptedPayload": encrypted as Any,
        "sortIndex": sortIndex as Any,
        "isArchived": false as Any,
        "createdAt": Date() as Any,
        "updatedAt": Date() as Any
    ])
    let _ = try await request("PATCH", path: "shared_wishlists/\(wishlistID)/items/\(itemID)", body: ["fields": fields])
}
```

Аналогично `createPersonalItem` — те же новые параметры.

- [ ] **Step 5: Update `parseEncryptedItemFromDoc` — читать новые поля**

Найти функцию `parseEncryptedItemFromDoc(...)` (~строка 885) и обновить парсинг:

```swift
private func parseEncryptedItemFromDoc(_ doc: [String: Any], key: SymmetricKey) throws -> SharedItemInfo {
    guard let docName = doc["name"] as? String else { throw FirestoreError.notFound }
    let itemID = documentID(from: docName)
    guard let fields = doc["fields"] as? [String: Any] else { throw FirestoreError.notFound }
    let parsed = parseFields(fields)
    guard let payloadData = parsed["encryptedPayload"] as? Data else {
        throw FirestoreError.notFound
    }
    let content = try EncryptionService.decrypt(payloadData, using: key)

    let probationISO = content["probationEndAt"] as? String
    let probation: Date? = probationISO.flatMap { ISO8601DateFormatter().date(from: $0) }

    return SharedItemInfo(
        itemID: itemID,
        name: content["name"] as? String ?? "",
        tier: content["tier"] as? String ?? "maybe",
        price: content["price"] as? Double,
        priceMax: content["priceMax"] as? Double,
        currency: content["currency"] as? String ?? "RUB",
        url: content["url"] as? String,
        coverEmoji: content["coverEmoji"] as? String,
        coverImageData: EncryptionService.dataField(content, "coverImageData"),
        linkMetadataData: EncryptionService.dataField(content, "linkMeta"),
        descriptionText: content["description"] as? String,
        probationEndAt: probation,
        addedByUID: content["addedByUID"] as? String,
        addedByName: content["addedByName"] as? String,
        sortIndex: parsed["sortIndex"] as? Double ?? 0,
        isArchived: parsed["isArchived"] as? Bool ?? false
    )
}
```

- [ ] **Step 6: Update `SharedItemInfo` struct — добавить новые поля**

Найти `struct SharedItemInfo` (~строка 30) и обновить:

```swift
struct SharedItemInfo: Sendable {
    let itemID: String
    let name: String
    let tier: String
    let price: Double?
    let priceMax: Double?           // NEW
    let currency: String
    let url: String?
    let coverEmoji: String?
    let coverImageData: Data?       // NEW
    let linkMetadataData: Data?     // NEW
    let descriptionText: String?    // NEW
    let probationEndAt: Date?       // NEW
    let addedByUID: String?
    let addedByName: String?
    let sortIndex: Double
    let isArchived: Bool
}
```

- [ ] **Step 7: Compile check (expect failure for callsites)**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -40
```

Expected: errors в DataService и других call sites — это OK, фиксим в Task 5.

- [ ] **Step 8: Commit (без push — пока не работает)**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Services/FirestoreService.swift && git commit -m "firestore: preserve-unknown-keys in updateXxx + new payload fields (description, coverImage, linkMeta, probationEnd, priceMax)"
```

---

## Task 3: FirestoreService — preserve-unknown-keys в `updateSharedWishlist` + `updatePersonalWishlist`

**Files:**
- Modify: `IWish/Services/FirestoreService.swift:245-260, 456-472`

- [ ] **Step 1: Replace `updateSharedWishlist`**

```swift
func updateSharedWishlist(
    wishlistID: String,
    name: String,
    emoji: String?,
    coverImageData: Data?,
    gradientHue: Double?,
    ownerName: String?,
    key: SymmetricKey
) async throws {
    var existingPayload: [String: Any] = [:]
    if let existingDoc = try? await request("GET", path: "shared_wishlists/\(wishlistID)"),
       let fields = existingDoc["fields"] as? [String: Any],
       let encryptedData = parseFields(fields)["encryptedPayload"] as? Data,
       let decrypted = try? EncryptionService.decrypt(encryptedData, using: key) {
        existingPayload = decrypted
    }

    let updates: [String: Any?] = [
        "name": name,
        "coverEmoji": emoji,
        "coverImageData": coverImageData,
        "ownerName": ownerName
    ]

    var merged = existingPayload
    for (k, v) in updates {
        if let v {
            if let d = v as? Data { merged[k] = d.base64EncodedString() } else { merged[k] = v }
        } else {
            merged.removeValue(forKey: k)
        }
    }

    let encrypted = try EncryptionService.encrypt(merged, using: key)

    var data: [String: Any?] = [
        "encryptedPayload": encrypted,
        "updatedAt": Date() as Any
    ]
    var maskFields = ["encryptedPayload", "updatedAt"]
    if let gradientHue {
        data["gradientHue"] = gradientHue
        maskFields.append("gradientHue")
    }
    let maskPaths = maskFields.map { "updateMask.fieldPaths=\($0)" }.joined(separator: "&")
    let fields = toFields(data)
    let _ = try await request("PATCH", path: "shared_wishlists/\(wishlistID)?\(maskPaths)", body: ["fields": fields])
}
```

- [ ] **Step 2: Replace `updatePersonalWishlist`**

```swift
func updatePersonalWishlist(
    uid: String,
    wishlistID: String,
    name: String,
    emoji: String?,
    coverImageData: Data?,
    gradientHue: Double?,
    key: SymmetricKey
) async throws {
    var existingPayload: [String: Any] = [:]
    if let existingDoc = try? await request("GET", path: "users/\(uid)/wishlists/\(wishlistID)"),
       let fields = existingDoc["fields"] as? [String: Any],
       let encryptedData = parseFields(fields)["encryptedPayload"] as? Data,
       let decrypted = try? EncryptionService.decrypt(encryptedData, using: key) {
        existingPayload = decrypted
    }

    let updates: [String: Any?] = [
        "name": name,
        "coverEmoji": emoji,
        "coverImageData": coverImageData
    ]

    var merged = existingPayload
    for (k, v) in updates {
        if let v {
            if let d = v as? Data { merged[k] = d.base64EncodedString() } else { merged[k] = v }
        } else {
            merged.removeValue(forKey: k)
        }
    }

    let encrypted = try EncryptionService.encrypt(merged, using: key)

    var data: [String: Any?] = [
        "encryptedPayload": encrypted,
        "updatedAt": Date() as Any
    ]
    var maskFields = ["encryptedPayload", "updatedAt"]
    if let gradientHue {
        data["gradientHue"] = gradientHue
        maskFields.append("gradientHue")
    }
    let maskPaths = maskFields.map { "updateMask.fieldPaths=\($0)" }.joined(separator: "&")
    let fields = toFields(data)
    let _ = try await request("PATCH", path: "users/\(uid)/wishlists/\(wishlistID)?\(maskPaths)", body: ["fields": fields])
}
```

- [ ] **Step 3: Update `createPersonalWishlist` и `createSharedWishlist` — добавить gradientHue**

`createPersonalWishlist`: добавить параметр `gradientHue: Double? = nil`. Записывать в data:

```swift
var data: [String: Any?] = [
    "encryptedPayload": encrypted,
    "gradientSeed": gradientSeed as Any,
    "isArchived": false as Any,
    "createdAt": Date() as Any,
    "updatedAt": Date() as Any
]
if let gradientHue { data["gradientHue"] = gradientHue }
```

Аналогично `createSharedWishlist`: добавить параметр `gradientHue: Double? = nil`.

- [ ] **Step 4: Update `fetchPersonalWishlists` — читать gradientHue**

В функции `fetchPersonalWishlists`, в return tuple добавить gradientHue:

```swift
func fetchPersonalWishlists(uid: String, keyProvider: (String) -> SymmetricKey?) async throws
    -> [(id: String, name: String, emoji: String?, coverImageData: Data?, gradientSeed: Int, gradientHue: Double?, isArchived: Bool)]
```

И при парсинге:

```swift
let hue = parsed["gradientHue"] as? Double
results.append((id: docID, name: name, emoji: emoji, coverImageData: coverImageData, gradientSeed: seed, gradientHue: hue, isArchived: isArchived))
```

- [ ] **Step 5: Update `SharedWishlistInfo` struct — добавить gradientHue**

```swift
struct SharedWishlistInfo: Sendable {
    let wishlistID: String
    let name: String
    let coverEmoji: String?
    let coverImageData: Data?
    let ownerUID: String
    let ownerName: String?
    let gradientSeed: Int
    let gradientHue: Double?           // NEW
    let isArchived: Bool
    let members: [(userUID: String, role: String, userName: String?)]  // NEW userName
    let items: [SharedItemInfo]
}
```

И в `fetchSharedWishlist` парсить `parsed["gradientHue"] as? Double`.

- [ ] **Step 6: Compile check (expect failure)**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -40
```

Expected: ошибки call sites (DataService) — фиксим в Task 5.

- [ ] **Step 7: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Services/FirestoreService.swift && git commit -m "firestore: preserve-unknown-keys in updateXxxWishlist + gradientHue field + member userName"
```

---

## Task 4: FirestoreService — memberships userName + rate limit cap

**Files:**
- Modify: `IWish/Services/InputLimits.swift`
- Modify: `IWish/Services/FirestoreService.swift` (joinWishlist, fetchSharedWishlist members parsing, updateMembershipUserName новый метод)

- [ ] **Step 1: Update InputLimits cap**

В `IWish/Services/InputLimits.swift:22`:

```swift
static let maxFirestoreRequestsPerMinute = 300  // было 60
```

- [ ] **Step 2: Update `joinWishlist` — добавить userName**

```swift
func joinWishlist(wishlistID: String, userUID: String, userName: String, role: String, canInvite: Bool = false) async throws {
    let membershipID = "\(userUID)_\(wishlistID)"
    let fields = toFields([
        "wishlistID": wishlistID,
        "userUID": userUID,
        "userName": userName,
        "role": role,
        "canInvite": canInvite as Any,
        "joinedAt": Date() as Any
    ])
    let _ = try await request("PATCH", path: "memberships/\(membershipID)", body: ["fields": fields])
}
```

- [ ] **Step 3: Add new `updateMembershipUserName` method**

```swift
/// Обновить только userName в существующих memberships текущего пользователя.
/// Используется при backfill после миграции имени.
func updateMembershipUserName(wishlistID: String, userUID: String, userName: String) async throws {
    let membershipID = "\(userUID)_\(wishlistID)"
    let fields = toFields(["userName": userName])
    let _ = try await request(
        "PATCH",
        path: "memberships/\(membershipID)?updateMask.fieldPaths=userName",
        body: ["fields": fields]
    )
}
```

- [ ] **Step 4: Update members parsing в `fetchSharedWishlist`**

В функции `fetchSharedWishlist`, найти секцию parsing memberships (~строка 611-617):

```swift
let membersResults = try await runQuery(collectionId: "memberships", field: "wishlistID", op: "EQUAL", value: wishlistID)
let members: [(userUID: String, role: String, userName: String?)] = membersResults.compactMap { entry in
    guard let doc = entry["document"] as? [String: Any],
          let f = doc["fields"] as? [String: Any] else { return nil }
    let d = parseFields(f)
    let userName = d["userName"] as? String
    return (
        userUID: d["userUID"] as? String ?? "",
        role: d["role"] as? String ?? "viewer",
        userName: userName?.isEmpty == false ? userName : nil
    )
}
```

И обновить SharedWishlistInfo в return — `members: members` уже совпадает с новой struct sig.

- [ ] **Step 5: Compile check**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -40
```

Expected: ошибки в JoinWishlistSheet и ParticipantsView — фиксим в их секциях.

- [ ] **Step 6: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Services/FirestoreService.swift IWish/Services/InputLimits.swift && git commit -m "firestore: membership userName field + rate cap 300/min"
```

---

## Task 5: DataService — обновить call sites + новые параметры + backfill helper

**Files:**
- Modify: `IWish/Services/DataService.swift`

- [ ] **Step 1: Update `addItem` signature и body**

Найти `func addItem(...)` (~строка 438) и заменить на:

```swift
func addItem(
    to wishlistID: String,
    name: String,
    tier: ItemTier,
    price: Double?,
    priceMax: Double? = nil,
    currency: String,
    url: String?,
    emoji: String?,
    sortIndex: Double,
    descriptionText: String? = nil,
    probationEndAt: Date? = nil,
    coverImageData: Data? = nil,
    linkMetadataData: Data? = nil
) async throws -> Item {
    let currentUID = try uid
    let currentUserName = auth.bestDisplayName()

    let item = Item(
        name: name,
        tier: tier,
        sortIndex: sortIndex,
        currency: currency,
        price: price,
        priceMax: priceMax,
        descriptionText: descriptionText,
        url: url,
        coverImageData: coverImageData,
        coverEmoji: emoji,
        addedByUID: currentUID,
        addedByName: currentUserName
    )
    item.linkMetadataData = linkMetadataData
    if let probationEndAt {
        item.probationEndAt = probationEndAt
    }
    let itemID = item.id.uuidString

    isSyncing = true
    syncError = nil

    guard let uuid = UUID(uuidString: wishlistID) else { throw FirestoreService.FirestoreError.notFound }
    let descriptor = FetchDescriptor<Wishlist>(predicate: #Predicate { $0.id == uuid })
    guard let wishlist = try modelContext.fetch(descriptor).first else {
        isSyncing = false
        throw FirestoreService.FirestoreError.notFound
    }

    let activeItemsCount = (wishlist.items ?? []).filter { !$0.isArchived }.count
    guard activeItemsCount < InputLimits.maxItemsPerWishlist else {
        isSyncing = false
        throw FirestoreService.FirestoreError.requestFailed(
            "В списке достигнут лимит — \(InputLimits.maxItemsPerWishlist) желаний. Удалите ненужные."
        )
    }

    guard let key = KeychainService.load(for: wishlistID) else {
        isSyncing = false
        throw FirestoreService.FirestoreError.requestFailed("Ключ шифрования не найден")
    }

    do {
        if wishlistIsShared(wishlist), let sharedID = wishlist.sharedWishlistID {
            try await verifyEditorRole(wishlistID: sharedID)
            try await firestore.createSharedItem(
                wishlistID: sharedID,
                itemID: itemID,
                name: name,
                tier: tier.rawValue,
                price: price,
                priceMax: priceMax,
                currency: currency,
                url: url,
                emoji: emoji,
                sortIndex: sortIndex,
                descriptionText: descriptionText,
                coverImageData: coverImageData,
                linkMetadataData: linkMetadataData,
                probationEndAt: probationEndAt,
                addedByUID: currentUID,
                addedByName: currentUserName,
                key: key
            )
        } else {
            try await firestore.createPersonalItem(
                uid: currentUID,
                wishlistID: wishlistID,
                itemID: itemID,
                name: name,
                tier: tier.rawValue,
                price: price,
                priceMax: priceMax,
                currency: currency,
                url: url,
                emoji: emoji,
                sortIndex: sortIndex,
                descriptionText: descriptionText,
                coverImageData: coverImageData,
                linkMetadataData: linkMetadataData,
                probationEndAt: probationEndAt,
                addedByUID: currentUID,
                addedByName: currentUserName,
                key: key
            )
        }
    } catch {
        isSyncing = false
        throw error
    }

    item.wishlist = wishlist
    modelContext.insert(item)
    try? modelContext.save()
    isSyncing = false
    return item
}
```

- [ ] **Step 2: Update `updateItem` signature и body**

Найти `func updateItem(...)` (~строка 508) и заменить:

```swift
func updateItem(
    id: String,
    wishlistID: String,
    name: String,
    tier: ItemTier,
    price: Double?,
    priceMax: Double? = nil,
    currency: String,
    url: String?,
    emoji: String?,
    sortIndex: Double,
    isArchived: Bool,
    descriptionText: String? = nil,
    coverImageData: Data? = nil,
    linkMetadataData: Data? = nil,
    probationEndAt: Date? = nil
) async throws {
    let currentUID = try uid

    await acquireLock()
    isSyncing = true
    syncError = nil
    defer { releaseLock() }

    guard let itemUUID = UUID(uuidString: id) else { throw FirestoreService.FirestoreError.notFound }
    let descriptor = FetchDescriptor<Item>(predicate: #Predicate { $0.id == itemUUID })
    guard let item = try modelContext.fetch(descriptor).first else {
        isSyncing = false
        throw FirestoreService.FirestoreError.notFound
    }

    let wishlist = item.wishlist
    let isShared = wishlist.map { wishlistIsShared($0) } ?? false
    let sharedID = wishlist?.sharedWishlistID

    guard let key = KeychainService.load(for: wishlistID) else {
        isSyncing = false
        throw FirestoreService.FirestoreError.requestFailed("Ключ шифрования не найден")
    }

    let preservedAddedByUID = item.addedByUID
    let preservedAddedByName = item.addedByName

    do {
        if isShared, let sharedID {
            try await verifyEditorRole(wishlistID: sharedID)
            try await firestore.updateSharedItem(
                wishlistID: sharedID,
                itemID: id,
                name: name,
                tier: tier.rawValue,
                price: price,
                priceMax: priceMax,
                currency: currency,
                url: url,
                emoji: emoji,
                sortIndex: sortIndex,
                isArchived: isArchived,
                descriptionText: descriptionText,
                coverImageData: coverImageData,
                linkMetadataData: linkMetadataData,
                probationEndAt: probationEndAt,
                addedByUID: preservedAddedByUID,
                addedByName: preservedAddedByName,
                key: key
            )
        } else {
            try await firestore.updatePersonalItem(
                uid: currentUID,
                wishlistID: wishlistID,
                itemID: id,
                name: name,
                tier: tier.rawValue,
                price: price,
                priceMax: priceMax,
                currency: currency,
                url: url,
                emoji: emoji,
                sortIndex: sortIndex,
                isArchived: isArchived,
                descriptionText: descriptionText,
                coverImageData: coverImageData,
                linkMetadataData: linkMetadataData,
                probationEndAt: probationEndAt,
                addedByUID: preservedAddedByUID,
                addedByName: preservedAddedByName,
                key: key
            )
        }
    } catch {
        isSyncing = false
        throw error
    }

    item.name = name
    item.tier = tier
    item.priceValue = price
    item.priceMaxValue = priceMax
    item.currency = currency
    item.url = url
    item.coverEmoji = emoji
    item.sortIndex = sortIndex
    item.isArchived = isArchived
    item.descriptionText = descriptionText
    item.coverImageData = coverImageData
    item.linkMetadataData = linkMetadataData
    item.probationEndAt = probationEndAt
    item.updatedAt = .now
    try? modelContext.save()
    isSyncing = false
}
```

- [ ] **Step 3: Update `archiveItem` — pass new params**

В `archiveItem` тоже идёт через `updateSharedItem/updatePersonalItem`. Заменить вызовы (~строки 627-635):

```swift
if isShared, let sharedID {
    try await verifyEditorRole(wishlistID: sharedID)
    try await firestore.updateSharedItem(
        wishlistID: sharedID,
        itemID: id,
        name: item.name,
        tier: item.tier.rawValue,
        price: item.priceValue,
        priceMax: item.priceMaxValue,
        currency: item.currency,
        url: item.url,
        emoji: item.coverEmoji,
        sortIndex: item.sortIndex,
        isArchived: true,
        descriptionText: item.descriptionText,
        coverImageData: item.coverImageData,
        linkMetadataData: item.linkMetadataData,
        probationEndAt: item.probationEndAt,
        addedByUID: item.addedByUID,
        addedByName: item.addedByName,
        key: key
    )
} else {
    try await firestore.updatePersonalItem(
        uid: currentUID,
        wishlistID: wishlistID,
        itemID: id,
        name: item.name,
        tier: item.tier.rawValue,
        price: item.priceValue,
        priceMax: item.priceMaxValue,
        currency: item.currency,
        url: item.url,
        emoji: item.coverEmoji,
        sortIndex: item.sortIndex,
        isArchived: true,
        descriptionText: item.descriptionText,
        coverImageData: item.coverImageData,
        linkMetadataData: item.linkMetadataData,
        probationEndAt: item.probationEndAt,
        addedByUID: item.addedByUID,
        addedByName: item.addedByName,
        key: key
    )
}
```

- [ ] **Step 4: Update `createWishlist` — pass gradientHue (init nil)**

В `createWishlist` (~строка 184):

```swift
try await firestore.createPersonalWishlist(
    uid: currentUID,
    wishlistID: wishlistID,
    name: name,
    emoji: emoji,
    gradientSeed: seed,
    gradientHue: nil,  // первоначальное создание — нет hue
    coverImageData: coverImageData,
    key: key
)
```

- [ ] **Step 5: Update `updateWishlist` — pass gradientHue + coverImageData**

В `updateWishlist` (~строка 248) добавить параметр `gradientHue: Double?`:

```swift
func updateWishlist(id: String, name: String, emoji: String?, coverImageData: Data? = nil, gradientHue: Double? = nil) async throws {
    // ... existing
    if wishlistIsShared(wishlist), let sharedID = wishlist.sharedWishlistID {
        // owner check как было
        try await firestore.updateSharedWishlist(
            wishlistID: sharedID,
            name: name,
            emoji: emoji,
            coverImageData: coverImageData,
            gradientHue: gradientHue,
            ownerName: auth.userName,
            key: key
        )
    } else {
        try await firestore.updatePersonalWishlist(
            uid: currentUID,
            wishlistID: id,
            name: name,
            emoji: emoji,
            coverImageData: coverImageData,
            gradientHue: gradientHue,
            key: key
        )
    }

    wishlist.name = name
    wishlist.coverEmoji = emoji
    wishlist.coverImageData = coverImageData
    if let gradientHue { wishlist.gradientHue = gradientHue }
    wishlist.updatedAt = .now
    // ...
}
```

- [ ] **Step 6: Update `mergeItems` — пробрасывать новые поля**

В `mergeItems` (~строка 918), внутри `if let local = ...` для существующего:

```swift
if let local = localByID[remote.itemID] {
    local.name = remote.name
    local.tier = tier
    local.priceValue = remote.price
    local.priceMaxValue = remote.priceMax
    local.currency = remote.currency
    local.url = remote.url
    local.coverEmoji = remote.coverEmoji
    if let img = remote.coverImageData { local.coverImageData = img }  // не обнуляем если remote nil
    if let lm = remote.linkMetadataData { local.linkMetadataData = lm }
    if let desc = remote.descriptionText { local.descriptionText = desc }
    if let prob = remote.probationEndAt { local.probationEndAt = prob }
    local.sortIndex = remote.sortIndex
    local.isArchived = remote.isArchived
    if let uid = remote.addedByUID { local.addedByUID = uid }
    if let nm = remote.addedByName { local.addedByName = nm }
    local.updatedAt = .now
} else {
    let item = Item(
        name: remote.name,
        tier: tier,
        sortIndex: remote.sortIndex,
        currency: remote.currency,
        price: remote.price,
        priceMax: remote.priceMax,
        descriptionText: remote.descriptionText,
        url: remote.url,
        coverImageData: remote.coverImageData,
        coverEmoji: remote.coverEmoji,
        addedByUID: remote.addedByUID,
        addedByName: remote.addedByName
    )
    item.linkMetadataData = remote.linkMetadataData
    item.probationEndAt = remote.probationEndAt
    item.isArchived = remote.isArchived
    item.wishlist = wishlist
    if let itemUUID = UUID(uuidString: remote.itemID) {
        item.id = itemUUID
    }
    modelContext.insert(item)
}
```

- [ ] **Step 7: Update refreshWishlists — учесть gradientHue**

В `refreshWishlists` (~строка 700), при mapping remote в local (новый и existing):

```swift
local.gradientSeed = r.gradientSeed
local.gradientHue = r.gradientHue
```

И при создании нового Wishlist:

```swift
let newWL = Wishlist(
    name: r.name,
    coverImageData: r.coverImageData,
    coverEmoji: r.emoji,
    isArchived: r.isArchived,
    gradientSeed: r.gradientSeed,
    gradientHue: r.gradientHue
)
```

Аналогично для shared (~строка 786):

```swift
local.gradientSeed = info.gradientSeed
local.gradientHue = info.gradientHue
```

И:

```swift
let newWL = Wishlist(
    name: info.name,
    coverImageData: info.coverImageData,
    coverEmoji: info.coverEmoji,
    ownerRecordID: info.ownerUID,
    isShared: true,
    isArchived: info.isArchived,
    sharedWishlistID: info.wishlistID,
    gradientSeed: info.gradientSeed,
    gradientHue: info.gradientHue
)
```

- [ ] **Step 8: Add `runV11Backfill()` method**

В конце DataService (перед последней `}`):

```swift
// MARK: - v1.1 Migration backfill

/// One-time backfill при первом старте v1.1: пройти по local items с непустыми
/// "missing" полями (description/coverImage/linkMeta/probationEnd) и переслать в Firestore
/// чтобы они попали в encryptedPayload. Идемпотентно. Маркер UserDefaults.
func runV11BackfillIfNeeded() async {
    let key = "iwish_v11_backfill_done"
    guard !UserDefaults.standard.bool(forKey: key) else { return }
    guard auth.uid != nil else { return }

    let items = (try? modelContext.fetch(FetchDescriptor<Item>())) ?? []
    var allOK = true

    for item in items {
        let needsBackfill = (item.descriptionText?.isEmpty == false)
            || item.coverImageData != nil
            || item.linkMetadataData != nil
            || item.probationEndAt != nil
        guard needsBackfill else { continue }
        guard let wishlist = item.wishlist else { continue }

        do {
            try await updateItem(
                id: item.id.uuidString,
                wishlistID: wishlist.id.uuidString,
                name: item.name,
                tier: item.tier,
                price: item.priceValue,
                priceMax: item.priceMaxValue,
                currency: item.currency,
                url: item.url,
                emoji: item.coverEmoji,
                sortIndex: item.sortIndex,
                isArchived: item.isArchived,
                descriptionText: item.descriptionText,
                coverImageData: item.coverImageData,
                linkMetadataData: item.linkMetadataData,
                probationEndAt: item.probationEndAt
            )
        } catch {
            dsLog.warning("v1.1 backfill: failed for item \(item.id.uuidString, privacy: .public): \(error.localizedDescription, privacy: .public)")
            allOK = false
        }
    }

    // Backfill memberships userName — у меня могут быть memberships с пустым userName
    if let myUID = auth.uid, let myName = auth.userName, !myName.isEmpty {
        do {
            let memberships = try await firestore.fetchMyMemberships(userUID: myUID)
            for m in memberships {
                try? await firestore.updateMembershipUserName(wishlistID: m.wishlistID, userUID: myUID, userName: myName)
            }
        } catch {
            dsLog.warning("v1.1 backfill: memberships userName failed: \(error.localizedDescription, privacy: .public)")
            allOK = false
        }
    }

    if allOK {
        UserDefaults.standard.set(true, forKey: key)
        dsLog.info("v1.1 backfill: completed and marked done")
    } else {
        dsLog.info("v1.1 backfill: partial — will retry on next start")
    }
}
```

- [ ] **Step 9: Compile check**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -40
```

Expected: остаются ошибки в Views (AddItemSheet, EditItemSheet, JoinWishlistSheet, ParticipantsView) — фиксим в их Tasks.

- [ ] **Step 10: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Services/DataService.swift && git commit -m "data: pass new fields to Firestore + v1.1 backfill helper"
```

---

## Task 6: AuthService — name gate

**Files:**
- Modify: `IWish/Services/AuthService.swift`

- [ ] **Step 1: Add `requiresNameRecovery` state**

В `AuthService` после существующих `var`:

```swift
@Observable
@MainActor
final class AuthService: NSObject {
    var userName: String?
    var isLoading: Bool = true
    var requiresNameRecovery: Bool = false  // NEW
    // ...
}
```

- [ ] **Step 2: Migrate legacy "Пользователь" в init**

В `init()` после миграции UserDefaults legacy (~строка 28):

```swift
override init() {
    super.init()

    // Migration: перенести legacy UserDefaults["auth_userName"] в iCloud Keychain
    if KeychainService.loadUserName() == nil,
       let legacyName = UserDefaults.standard.string(forKey: "auth_userName"),
       !legacyName.isEmpty {
        KeychainService.saveUserName(legacyName)
        UserDefaults.standard.removeObject(forKey: "auth_userName")
    }

    // v1.1: стереть legacy "Пользователь" мусор (был fallback в старых версиях)
    if KeychainService.loadUserName() == "Пользователь" {
        KeychainService.deleteUserName()
        UserDefaults.standard.removeObject(forKey: "auth_userName")
    }

    userName = KeychainService.loadUserName()
        ?? UserDefaults.standard.string(forKey: "auth_userName")

    // existing logic...
}
```

- [ ] **Step 3: Update `verifyAndRestore` — выставлять `requiresNameRecovery`**

```swift
private func verifyAndRestore(uid: String) async {
    _isAppleSignedIn = true
    await refreshToken()

    if userName == nil {
        userName = KeychainService.loadUserName()
    }

    if userName == nil, let dn = Auth.auth().currentUser?.displayName, !dn.isEmpty {
        userName = dn
        KeychainService.saveUserName(dn)
        print("[Auth] Restored name from Firebase displayName: \(dn)")
    }

    // v1.1: если имя так и не получилось — требуем re-auth
    if userName == nil || userName == "Пользователь" {
        requiresNameRecovery = true
        print("[Auth] No usable name after restore — requiring name recovery")
    }

    print("[Auth] Restored: \(uid), name: \(userName ?? "nil")")
}
```

- [ ] **Step 4: Update `handleSignInWithApple` — gate без имени**

Заменить блок `// 1. Try Apple credential ... // 4. Try email as last resort // let finalName = ...`:

```swift
var resolvedName: String?

// 1. Try Apple credential (только при первом sign-in c этим Apple ID)
if let givenName = credential.fullName?.givenName {
    resolvedName = [givenName, credential.fullName?.familyName]
        .compactMap { $0 }
        .joined(separator: " ")
}

// 2. Try Firebase displayName
if resolvedName == nil, let dn = authResult.user.displayName, !dn.isEmpty {
    resolvedName = dn
}

// 3. Try iCloud Keychain (если юзер уже логинился на этом или другом девайсе)
if resolvedName == nil {
    let kc = KeychainService.loadUserName()
    if kc != nil && kc != "Пользователь" {
        resolvedName = kc
    }
}

// 4. Email as last resort
if resolvedName == nil {
    if let email = authResult.user.email, !email.isEmpty {
        resolvedName = email.components(separatedBy: "@").first
    }
}

if let finalName = resolvedName, !finalName.isEmpty, finalName != "Пользователь" {
    userName = finalName
    KeychainService.saveUserName(finalName)
    requiresNameRecovery = false
} else {
    // Имя получить не удалось — пускать нельзя.
    userName = nil
    KeychainService.deleteUserName()
    requiresNameRecovery = true
}
```

- [ ] **Step 5: Update `signOut` — clear recovery flag**

```swift
func signOut() throws {
    try Auth.auth().signOut()
    _uid = nil
    _idToken = nil
    _refreshToken = nil
    _isAppleSignedIn = false
    userName = nil
    requiresNameRecovery = false
    KeychainService.deleteUserName()
    UserDefaults.standard.removeObject(forKey: "auth_userName")
}
```

- [ ] **Step 6: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -20
```

Expected: AuthService должен компилироваться без новых ошибок (ошибки могут оставаться в Views — это OK).

- [ ] **Step 7: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Services/AuthService.swift && git commit -m "auth: name gate — no fallback to 'Пользователь', require re-auth if missing"
```

---

## Task 7: NameRecoveryView (новый view)

**Files:**
- Create: `IWish/Views/NameRecoveryView.swift`

- [ ] **Step 1: Создать NameRecoveryView**

Содержимое `IWish/Views/NameRecoveryView.swift`:

```swift
import SwiftUI

/// Показывается когда у залогиненного юзера не удалось получить имя
/// (Apple credential.fullName == nil + Firebase displayName пуст + Keychain пуст).
/// Юзер должен выйти и зайти заново, удалив "Sign in with Apple" в настройках iPhone.
struct NameRecoveryView: View {
    @Environment(\.appServices) private var services
    @Environment(\.toast) private var toast
    @State private var isSigningOut = false

    var body: some View {
        VStack(spacing: 24) {
            Image(systemName: "person.crop.circle.badge.exclamationmark")
                .font(.system(size: 64))
                .foregroundStyle(.tint)

            VStack(spacing: 12) {
                Text("Не удалось получить ваше имя")
                    .font(.title2.weight(.semibold))
                    .multilineTextAlignment(.center)

                Text("Apple Sign In не передал имя при входе. Это происходит при повторной авторизации — Apple отдаёт имя только в первый раз.")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
                    .multilineTextAlignment(.center)

                Text("Чтобы починить:\n1. Откройте Настройки → Apple ID → Вход с Apple\n2. Найдите I Wish и нажмите «Прекратить использовать»\n3. Вернитесь сюда и войдите заново")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
                    .multilineTextAlignment(.leading)
                    .padding(.top, 8)
            }
            .padding(.horizontal, 24)

            VStack(spacing: 12) {
                Button {
                    if let url = URL(string: "App-Prefs:APPLE_ACCOUNT") {
                        UIApplication.shared.open(url)
                    }
                } label: {
                    Label("Открыть настройки Apple ID", systemImage: "gearshape")
                        .frame(maxWidth: .infinity)
                }
                .buttonStyle(.borderedProminent)
                .controlSize(.large)

                Button {
                    isSigningOut = true
                    Task {
                        try? services.auth.signOut()
                        isSigningOut = false
                    }
                } label: {
                    Label("Выйти из аккаунта", systemImage: "rectangle.portrait.and.arrow.right")
                        .frame(maxWidth: .infinity)
                }
                .buttonStyle(.bordered)
                .controlSize(.large)
                .disabled(isSigningOut)
            }
            .padding(.horizontal, 24)

            Spacer()
        }
        .padding(.top, 60)
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Theme.background.ignoresSafeArea())
        .applyTheme()
    }
}

#Preview {
    NameRecoveryView()
}
```

- [ ] **Step 2: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -10
```

Expected: компилируется без новых ошибок.

- [ ] **Step 3: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Views/NameRecoveryView.swift && git commit -m "ui: NameRecoveryView for re-auth flow when name is missing"
```

---

## Task 8: RemoteConfigService + ForceUpdateBlockingView (Force Update infra)

**Files:**
- Create: `IWish/Services/RemoteConfigService.swift`
- Create: `IWish/Views/ForceUpdateBlockingView.swift`
- Modify: `IWish.xcodeproj/project.pbxproj` (добавить FirebaseRemoteConfig SDK)

- [ ] **Step 1: Add FirebaseRemoteConfig package dependency**

Открыть `IWish.xcodeproj` в Xcode, в "Package Dependencies" найти firebase-ios-sdk → отметить `FirebaseRemoteConfig`. Закрыть Xcode после добавления.

Альтернатива через CLI: edit pbxproj вручную — добавить FirebaseRemoteConfig в `packageProductDependencies` для main target.

```bash
# Проверить что добавилось
grep -i "RemoteConfig" /Users/rugyron/Documents/Xcode/IWish/IWish.xcodeproj/project.pbxproj | head
```

Expected: должна быть строка с `FirebaseRemoteConfig`.

- [ ] **Step 2: Создать RemoteConfigService.swift**

Содержимое `IWish/Services/RemoteConfigService.swift`:

```swift
import Foundation
import FirebaseRemoteConfig
import os.log

private let rcLog = Logger(subsystem: "RUGyron.IWish", category: "RemoteConfig")

@Observable
@MainActor
final class RemoteConfigService {
    var isLoading = true
    var minSupportedBuild: Int = 0
    var forceUpdateMessage: String?
    var fetchFailed = false

    private let rc = RemoteConfig.remoteConfig()

    init() {
        let settings = RemoteConfigSettings()
        // В Debug — мгновенно (для тестов). В Release Apple рекомендует >=3600 (час).
        #if DEBUG
        settings.minimumFetchInterval = 0
        #else
        settings.minimumFetchInterval = 3600
        #endif
        rc.configSettings = settings
        rc.setDefaults([
            "min_supported_build": 0 as NSNumber,
            "force_update_message": "" as NSString
        ])
    }

    func fetch() async {
        isLoading = true
        defer { isLoading = false }

        do {
            // Timeout 5 секунд — иначе при отсутствии сети юзер залипнет на splash.
            try await withTimeout(seconds: 5) { [weak self] in
                guard let self else { return }
                let _ = try await self.rc.fetchAndActivate()
            }

            minSupportedBuild = rc.configValue(forKey: "min_supported_build").numberValue.intValue
            let msg = rc.configValue(forKey: "force_update_message").stringValue
            forceUpdateMessage = msg.isEmpty ? nil : msg
            fetchFailed = false
            rcLog.info("RC fetched: minBuild=\(self.minSupportedBuild), msg=\(self.forceUpdateMessage ?? "nil", privacy: .public)")
        } catch {
            fetchFailed = true
            rcLog.warning("RC fetch failed: \(error.localizedDescription, privacy: .public)")
        }
    }

    var currentBuild: Int {
        Int(Bundle.main.infoDictionary?["CFBundleVersion"] as? String ?? "0") ?? 0
    }

    var requiresForceUpdate: Bool {
        guard !fetchFailed else { return false }
        return currentBuild < minSupportedBuild
    }
}

// MARK: - Timeout helper

@MainActor
private func withTimeout<T: Sendable>(seconds: Double, operation: @escaping @Sendable () async throws -> T) async throws -> T {
    try await withThrowingTaskGroup(of: T.self) { group in
        group.addTask {
            try await operation()
        }
        group.addTask {
            try await Task.sleep(for: .seconds(seconds))
            throw RemoteConfigTimeout.timedOut
        }
        guard let first = try await group.next() else {
            throw RemoteConfigTimeout.timedOut
        }
        group.cancelAll()
        return first
    }
}

private enum RemoteConfigTimeout: Error {
    case timedOut
}
```

- [ ] **Step 3: Создать ForceUpdateBlockingView.swift**

Содержимое `IWish/Views/ForceUpdateBlockingView.swift`:

```swift
import SwiftUI

struct ForceUpdateBlockingView: View {
    let message: String?

    var body: some View {
        VStack(spacing: 24) {
            Spacer()

            Image(systemName: "arrow.down.circle.fill")
                .font(.system(size: 72))
                .foregroundStyle(.tint)

            VStack(spacing: 12) {
                Text("Обновите I Wish")
                    .font(.title2.weight(.semibold))

                Text(message ?? "Установлена устаревшая версия. Обновитесь, чтобы продолжить.")
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
                    .multilineTextAlignment(.center)
            }
            .padding(.horizontal, 32)

            Spacer()

            Button {
                if let url = URL(string: "https://apps.apple.com/app/id6762267281") {
                    UIApplication.shared.open(url)
                }
            } label: {
                Text("Открыть App Store")
                    .frame(maxWidth: .infinity)
            }
            .buttonStyle(.borderedProminent)
            .controlSize(.large)
            .padding(.horizontal, 32)
            .padding(.bottom, 32)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Theme.background.ignoresSafeArea())
        .applyTheme()
    }
}

#Preview {
    ForceUpdateBlockingView(message: "Тестовое сообщение об обновлении")
}
```

- [ ] **Step 4: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -10
```

Expected: компилируется. Если падает на FirebaseRemoteConfig import — значит SDK не добавлен (вернуться к Step 1).

- [ ] **Step 5: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Services/RemoteConfigService.swift IWish/Views/ForceUpdateBlockingView.swift IWish.xcodeproj/project.pbxproj && git commit -m "rc: add Firebase Remote Config + force update blocking screen"
```

---

## Task 9: RootView — split view + force update gate + name gate

**Files:**
- Modify: `IWish/Views/RootView.swift`

- [ ] **Step 1: Прочитать существующий RootView**

```bash
cat /Users/rugyron/Documents/Xcode/IWish/IWish/Views/RootView.swift
```

Запомнить структуру (sign-in screen vs HomeView).

- [ ] **Step 2: Replace RootView body**

Заменить содержимое `IWish/Views/RootView.swift`:

```swift
import SwiftUI
import SwiftData

struct RootView: View {
    @Environment(\.appServices) private var services
    @Environment(\.horizontalSizeClass) private var sizeClass
    @State private var rc = RemoteConfigService()

    var body: some View {
        Group {
            if rc.isLoading {
                splash
            } else if rc.requiresForceUpdate {
                ForceUpdateBlockingView(message: rc.forceUpdateMessage)
            } else if !services.auth.isAuthenticated {
                SignInWithAppleSheet { result in
                    Task {
                        try? await services.auth.handleSignInWithApple(result: result)
                    }
                }
            } else if services.auth.requiresNameRecovery {
                NameRecoveryView()
            } else {
                mainContent
            }
        }
        .task {
            await rc.fetch()
        }
    }

    private var splash: some View {
        VStack {
            ProgressView().scaleEffect(1.4)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Theme.background.ignoresSafeArea())
    }

    @ViewBuilder
    private var mainContent: some View {
        if sizeClass == .regular {
            // iPad / iPhone landscape — split
            NavigationSplitView {
                HomeView()
                    .navigationSplitViewColumnWidth(min: 320, ideal: 380, max: 500)
            } detail: {
                emptyDetail
            }
            .navigationSplitViewStyle(.balanced)
        } else {
            // iPhone portrait — push stack
            NavigationStack {
                HomeView()
            }
        }
    }

    private var emptyDetail: some View {
        VStack(spacing: 12) {
            Image(systemName: "list.bullet.rectangle")
                .font(.system(size: 56))
                .foregroundStyle(.tertiary)
            Text("Выберите список")
                .font(.title3)
                .foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Theme.background.ignoresSafeArea())
    }
}
```

ПРИМЕЧАНИЕ: если существующий RootView имеет дополнительную логику (например настройки sheet или другой sign-in путь) — сохрани её, влей в эту структуру.

- [ ] **Step 3: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -10
```

Expected: компилируется (если HomeView не сломан).

- [ ] **Step 4: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Views/RootView.swift && git commit -m "ui: RootView with force-update + name-recovery + iPad split"
```

---

## Task 10: IWishApp — backfill trigger + RemoteConfig init

**Files:**
- Modify: `IWish/IWishApp.swift`

- [ ] **Step 1: Add v11 backfill trigger**

В `IWishApp` добавить вызов backfill после init контейнера:

```swift
@main
struct IWishApp: App {
    let container: ModelContainer
    @State private var pendingShareURL: String?
    @State private var showingJoinFromLink = false

    init() {
        FirebaseApp.configure()
        self.container = ModelContainerFactory.makeProductionContainer()
        configureAppearance()
        preheatKeyboard()
    }

    var body: some Scene {
        WindowGroup {
            RootView()
                .environment(\.appServices, .shared)
                .onOpenURL { url in
                    handleIncomingURL(url)
                }
                .sheet(isPresented: $showingJoinFromLink) {
                    JoinWishlistSheet(initialURL: pendingShareURL)
                }
                .task {
                    // v1.1 one-time migration backfill — гонится в фоне, не блокирует UI.
                    // updateItem использует preserve-unknown-keys → не теряет ничего.
                    await AppServices.shared.data?.runV11BackfillIfNeeded()
                }
        }
        .modelContainer(container)
    }

    // ... остальные методы как было
}
```

- [ ] **Step 2: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -10
```

Expected: компилируется.

- [ ] **Step 3: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/IWishApp.swift && git commit -m "app: trigger v1.1 backfill on first launch"
```

---

## Task 11: JoinWishlistSheet + ParticipantsView — userName в memberships

**Files:**
- Modify: `IWish/Views/JoinWishlistSheet.swift`
- Modify: `IWish/Views/ParticipantsView.swift`

- [ ] **Step 1: Update JoinWishlistSheet — pass userName в joinWishlist**

В `IWish/Views/JoinWishlistSheet.swift`, найти вызов `services.firestore.joinWishlist(...)` (~строка 217-222):

```swift
guard let userName = services.auth.userName, !userName.isEmpty else {
    showingInvitePreview = false
    toast.error("Не удалось получить ваше имя — войдите заново")
    return
}

try await services.firestore.joinWishlist(
    wishlistID: info.wishlistID,
    userUID: uid,
    userName: userName,
    role: info.role,
    canInvite: info.canInvite
)
```

- [ ] **Step 2: Update ParticipantsView — использовать member.userName**

В `IWish/Views/ParticipantsView.swift`, обновить `members` state type (~строка 11):

```swift
@State private var members: [(userUID: String, role: String, name: String)] = []
```

Это уже было. Изменить fetchMembers чтобы использовать `member.userName`:

```swift
// Имена members теперь приходят из memberships document (поле userName).
// Для self берём актуальное services.auth.userName.
let myUID = services.auth.uid
let myName = services.auth.userName
var resolved: [(userUID: String, role: String, name: String)] = []
for member in nonOwnerMembers {
    let name: String
    if member.userUID == myUID, let myName, !myName.isEmpty {
        name = myName
    } else if let memberName = member.userName, !memberName.isEmpty {
        name = memberName
    } else {
        name = "Участник"
    }
    resolved.append((userUID: member.userUID, role: member.role, name: name))
}
members = resolved
```

ВАЖНО: `nonOwnerMembers` теперь `[(userUID: String, role: String, userName: String?)]` (новый тип после Task 4) — компилятор должен подсветить.

- [ ] **Step 3: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -10
```

Expected: компилируется.

- [ ] **Step 4: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Views/JoinWishlistSheet.swift IWish/Views/ParticipantsView.swift && git commit -m "ui: real member names from memberships userName"
```

---

## Task 12: SettingsView — version без билда + Apple ID fallback

**Files:**
- Modify: `IWish/Views/SettingsView.swift`

- [ ] **Step 1: Replace `appVersion` computed property**

В `IWish/Views/SettingsView.swift:287-291`:

```swift
private var appVersion: String {
    Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "–"
}
```

(удалили build extraction и скобки)

- [ ] **Step 2: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -5
```

Expected: компилируется.

- [ ] **Step 3: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Views/SettingsView.swift && git commit -m "settings: version caption without build number"
```

---

## Task 13: Keyboard simplify в 4 sheet'ах + numeric input fixes

**Files:**
- Modify: `IWish/Views/AddItemSheet.swift`
- Modify: `IWish/Views/EditItemSheet.swift`
- Modify: `IWish/Views/AddWishlistSheet.swift`
- Modify: `IWish/Views/EditWishlistSheet.swift`

- [ ] **Step 1: AddItemSheet — удалить keyboard toolbar, добавить tap-dismiss, parsePrice Int**

В `AddItemSheet.swift`:

Удалить toolbar group (строки 57-64):
```swift
// УДАЛИТЬ
ToolbarItemGroup(placement: .keyboard) {
    Spacer()
    Button { ... } label: {
        Image(systemName: "keyboard.chevron.compact.down")
    }
}
```

Добавить tap-to-dismiss на Form. Заменить `Form { ... }` на:
```swift
Form {
    urlSection
    nameSection
    coverSection
    tierSection
    priceSection
    descriptionSection
    probationSection
}
.contentShape(Rectangle())
.onTapGesture {
    UIApplication.shared.sendAction(#selector(UIResponder.resignFirstResponder), to: nil, from: nil, for: nil)
}
```

Заменить `parsePrice` (строки 307-311):
```swift
private func parsePrice(_ string: String) -> Double? {
    let trimmed = string.trimmingCharacters(in: .whitespaces)
    guard !trimmed.isEmpty else { return nil }
    return Int(trimmed).map(Double.init)  // только Int
}
```

- [ ] **Step 2: EditItemSheet — те же правки + decimal display fix**

В `EditItemSheet.swift`:

Удалить keyboard toolbar (строки 82-89).

Добавить tap-to-dismiss на Form (как в AddItemSheet Step 1).

Replace `prefill()` строка 119:
```swift
priceString = item.priceValue.map { String(Int($0)) } ?? ""
```

Replace вызов в save (строка 145):
```swift
try await services.data.updateItem(
    id: item.id.uuidString,
    wishlistID: wishlistID,
    name: name.trimmingCharacters(in: .whitespaces),
    tier: tier,
    price: parsePrice(priceString),
    // ... остальные параметры
)
```

И добавить `parsePrice` helper:
```swift
private func parsePrice(_ string: String) -> Double? {
    let trimmed = string.trimmingCharacters(in: .whitespaces)
    guard !trimmed.isEmpty else { return nil }
    return Int(trimmed).map(Double.init)
}
```

- [ ] **Step 3: AddWishlistSheet — keyboard toolbar убрать, tap-dismiss**

Найти `ToolbarItemGroup(placement: .keyboard) { ... }` и удалить. Добавить `.contentShape(Rectangle()).onTapGesture { ... }` на Form.

- [ ] **Step 4: EditWishlistSheet — то же самое**

Аналогично.

- [ ] **Step 5: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -5
```

Expected: компилируется.

- [ ] **Step 6: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Views/AddItemSheet.swift IWish/Views/EditItemSheet.swift IWish/Views/AddWishlistSheet.swift IWish/Views/EditWishlistSheet.swift && git commit -m "keyboard: remove toolbar bar, use scrollDismissesKeyboard + tap-dismiss; price as Int only"
```

---

## Task 14: WishlistDetailView — tier emoji always + card radius 8 + range pricing display

**Files:**
- Modify: `IWish/Views/WishlistDetailView.swift`

- [ ] **Step 1: Tier emoji always в context menu (#6)**

В `WishlistDetailView.swift` строки 825-857 — replace inner Menu Buttons:

```swift
Menu {
    ForEach(ItemTier.allCases) { tier in
        Button {
            Task {
                do {
                    try await dataService?.updateItem(
                        id: item.id.uuidString,
                        wishlistID: wishlistID,
                        name: item.name,
                        tier: tier,
                        price: item.priceValue,
                        priceMax: item.priceMaxValue,
                        currency: item.currency,
                        url: item.url,
                        emoji: item.coverEmoji,
                        sortIndex: item.sortIndex,
                        isArchived: item.isArchived,
                        descriptionText: item.descriptionText,
                        coverImageData: item.coverImageData,
                        linkMetadataData: item.linkMetadataData,
                        probationEndAt: item.probationEndAt
                    )
                } catch {
                    toast.error(error.localizedDescription)
                }
            }
        } label: {
            HStack {
                Text("\(tier.emoji) \(tier.label)")
                if item.tier == tier {
                    Image(systemName: "checkmark")
                }
            }
        }
    }
} label: {
    Label("Изменить важность", systemImage: "arrow.up.arrow.down")
}
```

- [ ] **Step 2: Card corner radius 8**

В `itemRow` (строка 577) — найти `.listRowBackground(Color(...))`:

```swift
.listRowBackground(
    RoundedRectangle(cornerRadius: 8, style: .continuous)
        .fill(Color(.secondarySystemGroupedBackground))
)
.listRowSeparator(.hidden)
```

(заменить flat color на RoundedRectangle, добавить hidden separator)

- [ ] **Step 3: Range pricing display в карточке (topRow)**

Найти `topRow(for item:)` (~строка 621) и обновить price display:

```swift
private func topRow(for item: Item) -> some View {
    HStack(spacing: 6) {
        if wishlist.isShared, let author = authorLabel(for: item) {
            Text(author)
                .font(.caption2)
                .foregroundStyle(.secondary)
                .lineLimit(1)
            Text("·")
                .font(.caption2)
                .foregroundStyle(.tertiary)
        }
        Text(item.createdAt.formatted(.dateTime.day().month(.abbreviated)))
            .font(.caption2)
            .foregroundStyle(.tertiary)

        Spacer(minLength: 4)

        if let priceText = priceCardText(for: item) {
            Text(priceText)
                .font(.subheadline.weight(.semibold))
                .foregroundStyle(.primary)
                .layoutPriority(1)
                .lineLimit(1)
        }
    }
    .frame(height: 16)
}

/// На карточке показываем: точная цена, "до Y" для диапазона (только верхняя граница).
private func priceCardText(for item: Item) -> String? {
    switch item.priceMode {
    case .none: return nil
    case .exact(let p): return formatPrice(p, currency: item.currency)
    case .range(_, let max): return "до \(formatPrice(max, currency: item.currency))"
    }
}
```

- [ ] **Step 4: Range sum для tier-headers**

Найти `tierHeaderContent(tier:items:)` (~строка 473) и обновить:

```swift
private func tierHeaderContent(tier: ItemTier, items: [Item]) -> some View {
    let priceText = totalPriceText(for: items, currency: items.first?.currency ?? "RUB")
    let priceSuffix = priceText.map { " \u{00B7} \($0)" } ?? ""

    return HStack(spacing: 4) {
        Text(tier.emoji)
        Text("\(tier.label)\(priceSuffix) \u{00B7} \(items.count)")
    }
    .font(.subheadline.weight(.medium))
    .foregroundStyle(.secondary)
    .textCase(nil)
}

/// Сумма items, с учётом range. Если есть хотя бы один item-диапазон — возвращаем "Σmin — Σmax".
/// Если все exact — возвращаем простую сумму.
private func totalPriceText(for items: [Item], currency: String) -> String? {
    let priced = items.filter {
        switch $0.priceMode { case .none: return false; default: return true }
    }
    guard !priced.isEmpty else { return nil }

    let hasRange = priced.contains {
        if case .range = $0.priceMode { return true }
        return false
    }

    if hasRange {
        let totalMin = priced.reduce(0.0) { acc, item in
            switch item.priceMode {
            case .none: return acc
            case .exact(let p): return acc + p
            case .range(let min, _): return acc + min
            }
        }
        let totalMax = priced.reduce(0.0) { acc, item in
            switch item.priceMode {
            case .none: return acc
            case .exact(let p): return acc + p
            case .range(_, let max): return acc + max
            }
        }
        return "\(formatPrice(totalMin, currency: currency)) — \(formatPrice(totalMax, currency: currency))"
    } else {
        let total = priced.reduce(0.0) { acc, item in
            switch item.priceMode {
            case .exact(let p): return acc + p
            default: return acc
            }
        }
        return total > 0 ? formatPrice(total, currency: currency) : nil
    }
}
```

И в `wishlistHeaderRow` (~строка 412) заменить вычисление `totalActivePrice`:

```swift
HStack(spacing: 4) {
    Text(String(format: NSLocalizedString("%lld желаний", comment: ""), activeItems.count))
        .foregroundStyle(.secondary)
    if let totalText = totalPriceText(for: activeItems, currency: activeItems.first(where: { $0.priceValue != nil })?.currency ?? "RUB") {
        Text("\u{00B7} \(totalText)")
            .foregroundStyle(.secondary)
    }
}
```

(удалить старое `totalActivePrice` computed)

- [ ] **Step 5: Sort by price для диапазонов (avg)**

В `sortedItems(by: .price)` (~строка 561):

```swift
case .price:
    return activeItems.sorted { lhs, rhs in
        let lhsKey = avgPrice(lhs)
        let rhsKey = avgPrice(rhs)
        switch (lhsKey, rhsKey) {
        case let (.some(l), .some(r)): return l > r
        case (.some, .none): return true
        case (.none, .some): return false
        case (.none, .none): return lhs.sortIndex < rhs.sortIndex
        }
    }
```

И добавить helper:

```swift
private func avgPrice(_ item: Item) -> Double? {
    switch item.priceMode {
    case .none: return nil
    case .exact(let p): return p
    case .range(let min, let max): return (min + max) / 2
    }
}
```

- [ ] **Step 6: Reorder helper — pass new params в updateItem**

В `reorderItems(in tier:from:to:)` (~строка 487) — `updateItem` вызов:

```swift
try await services.data?.updateItem(
    id: item.id.uuidString,
    wishlistID: wishlist.id.uuidString,
    name: item.name,
    tier: item.tier,
    price: item.priceValue,
    priceMax: item.priceMaxValue,
    currency: item.currency,
    url: item.url,
    emoji: item.coverEmoji,
    sortIndex: item.sortIndex,
    isArchived: item.isArchived,
    descriptionText: item.descriptionText,
    coverImageData: item.coverImageData,
    linkMetadataData: item.linkMetadataData,
    probationEndAt: item.probationEndAt
)
```

- [ ] **Step 7: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -5
```

Expected: компилируется.

- [ ] **Step 8: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Views/WishlistDetailView.swift && git commit -m "ui: tier emoji always + card radius 8 + range pricing display + avg sort"
```

---

## Task 15: Range pricing UI в Add/Edit ItemSheet + ItemDetailSheet

**Files:**
- Modify: `IWish/Views/AddItemSheet.swift`
- Modify: `IWish/Views/EditItemSheet.swift`
- Modify: `IWish/Views/ItemDetailSheet.swift`

- [ ] **Step 1: Add PriceModeUI enum + state в AddItemSheet**

В начале AddItemSheet (после @State полей):

```swift
private enum PriceModeUI: String, CaseIterable, Identifiable {
    case exact, range
    var id: String { rawValue }
    var label: String { self == .exact ? "Точная" : "Диапазон" }
}

@State private var priceMode: PriceModeUI = .exact
@State private var priceMaxString: String = ""
```

Удалить existing `@State private var priceString: String = ""` и заменить на `@State private var priceMinString: String = ""` для семантической ясности.

- [ ] **Step 2: Replace priceSection в AddItemSheet**

```swift
private var priceSection: some View {
    Section("Цена") {
        Picker("Тип", selection: $priceMode) {
            ForEach(PriceModeUI.allCases) { mode in
                Text(mode.label).tag(mode)
            }
        }
        .pickerStyle(.segmented)
        .padding(.vertical, 4)

        if priceMode == .exact {
            HStack {
                TextField("0", text: $priceMinString)
                    .keyboardType(.numberPad)

                Picker("Валюта", selection: $currency) {
                    Text("\u{20BD}").tag("RUB")
                    Text("$").tag("USD")
                }
                .labelsHidden()
                .pickerStyle(.menu)
            }
        } else {
            HStack(spacing: 8) {
                TextField("От", text: $priceMinString)
                    .keyboardType(.numberPad)
                Text("—")
                    .foregroundStyle(.secondary)
                TextField("До", text: $priceMaxString)
                    .keyboardType(.numberPad)

                Picker("Валюта", selection: $currency) {
                    Text("\u{20BD}").tag("RUB")
                    Text("$").tag("USD")
                }
                .labelsHidden()
                .pickerStyle(.menu)
            }
        }
    }
}
```

- [ ] **Step 3: Update save() в AddItemSheet — pass priceMin + priceMax**

В `save()` метод:

```swift
let priceMin = parsePrice(priceMinString)
let priceMax: Double? = priceMode == .range ? parsePrice(priceMaxString) : nil

let item = try await services.data.addItem(
    to: wishlist.id.uuidString,
    name: trimmedName,
    tier: tier,
    price: priceMin,
    priceMax: priceMax,
    currency: currency.isEmpty ? "RUB" : currency,
    url: trimmedURL.isEmpty ? nil : trimmedURL,
    emoji: coverEmoji,
    sortIndex: nextSortIndex,
    descriptionText: descriptionText.isEmpty ? nil : descriptionText,
    probationEndAt: probEnd,
    coverImageData: coverImageData
)
```

- [ ] **Step 4: Same в EditItemSheet — добавить state + replace priceSection + update save**

В `EditItemSheet` добавить аналогичные state:

```swift
@State private var priceMinString: String = ""
@State private var priceMaxString: String = ""
@State private var priceMode: PriceModeUI = .exact

private enum PriceModeUI: String, CaseIterable, Identifiable {
    case exact, range
    var id: String { rawValue }
    var label: String { self == .exact ? "Точная" : "Диапазон" }
}
```

(удалить старое `@State private var priceString: String = ""`)

В `prefill()` обновить:

```swift
priceMinString = item.priceValue.map { String(Int($0)) } ?? ""
priceMaxString = item.priceMaxValue.map { String(Int($0)) } ?? ""
priceMode = item.priceMaxValue != nil ? .range : .exact
```

Replace priceSection в Form (строки 58-69):

```swift
Section("Цена") {
    Picker("Тип", selection: $priceMode) {
        ForEach(PriceModeUI.allCases) { mode in
            Text(mode.label).tag(mode)
        }
    }
    .pickerStyle(.segmented)
    .padding(.vertical, 4)

    if priceMode == .exact {
        HStack {
            TextField("0", text: $priceMinString)
                .keyboardType(.numberPad)
            Picker("Валюта", selection: $currency) {
                Text("\u{20BD}").tag("RUB")
                Text("$").tag("USD")
            }
            .labelsHidden()
            .pickerStyle(.menu)
        }
    } else {
        HStack(spacing: 8) {
            TextField("От", text: $priceMinString)
                .keyboardType(.numberPad)
            Text("—")
                .foregroundStyle(.secondary)
            TextField("До", text: $priceMaxString)
                .keyboardType(.numberPad)
            Picker("Валюта", selection: $currency) {
                Text("\u{20BD}").tag("RUB")
                Text("$").tag("USD")
            }
            .labelsHidden()
            .pickerStyle(.menu)
        }
    }
}
```

В `save()`:

```swift
let priceMin = parsePrice(priceMinString)
let priceMax: Double? = priceMode == .range ? parsePrice(priceMaxString) : nil

try await services.data.updateItem(
    id: item.id.uuidString,
    wishlistID: wishlistID,
    name: name.trimmingCharacters(in: .whitespaces),
    tier: tier,
    price: priceMin,
    priceMax: priceMax,
    currency: currency,
    url: urlString.isEmpty ? nil : urlString,
    emoji: coverEmoji,
    sortIndex: item.sortIndex,
    isArchived: item.isArchived,
    descriptionText: descriptionText.isEmpty ? nil : descriptionText,
    coverImageData: coverImageData,
    linkMetadataData: item.linkMetadataData,
    probationEndAt: probEnd
)
```

- [ ] **Step 5: ItemDetailSheet — отображать range "X — Y"**

В `IWish/Views/ItemDetailSheet.swift` найти `headerBlock` и заменить price display:

```swift
private var headerBlock: some View {
    VStack(alignment: .leading, spacing: 10) {
        tierBadge
        Text(item.name)
            .font(.title2.weight(.semibold))
            .frame(maxWidth: .infinity, alignment: .leading)
        if let priceText = priceFullText {
            Text(priceText)
                .font(.title3.weight(.bold))
                .foregroundStyle(.primary)
        }
    }
}

private var priceFullText: String? {
    switch item.priceMode {
    case .none: return nil
    case .exact(let p): return formatPrice(p, currency: item.currency)
    case .range(let min, let max):
        return "\(formatPrice(min, currency: item.currency)) — \(formatPrice(max, currency: item.currency))"
    }
}
```

- [ ] **Step 6: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -5
```

Expected: компилируется.

- [ ] **Step 7: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Views/AddItemSheet.swift IWish/Views/EditItemSheet.swift IWish/Views/ItemDetailSheet.swift && git commit -m "ui: range pricing UI in add/edit + detail display"
```

---

## Task 16: GradientHuePicker + integration в Add/EditWishlistSheet

**Files:**
- Create: `IWish/Views/Components/GradientHuePicker.swift`
- Modify: `IWish/Services/DefaultCoverGenerator.swift`
- Modify: `IWish/Views/Components/DefaultCoverView.swift`
- Modify: `IWish/Views/Components/WishlistTileView.swift`
- Modify: `IWish/Views/AddWishlistSheet.swift`
- Modify: `IWish/Views/EditWishlistSheet.swift`

- [ ] **Step 1: Add `colors(forHue:)` в DefaultCoverGenerator**

В `IWish/Services/DefaultCoverGenerator.swift` добавить (после существующих методов):

```swift
/// 3 цвета вокруг базового hue (±0.08 hue diff) для богатого MeshGradient.
static func colors(forHue hue: Double) -> [Color] {
    let h = max(0, min(1, hue))
    let h1 = (h - 0.08).truncatingRemainder(dividingBy: 1)
    let h3 = (h + 0.08).truncatingRemainder(dividingBy: 1)
    return [
        Color(hue: h1 < 0 ? h1 + 1 : h1, saturation: 0.75, brightness: 0.85),
        Color(hue: h,                    saturation: 0.65, brightness: 0.92),
        Color(hue: h3 > 1 ? h3 - 1 : h3, saturation: 0.70, brightness: 0.82)
    ]
}

/// Резолвер цвета для wishlist: hue если задан, иначе palette by seed.
static func colors(for wishlist: Wishlist) -> [Color] {
    if let hue = wishlist.gradientHue {
        return colors(forHue: hue)
    }
    return colors(forSeed: wishlist.gradientSeed)
}
```

- [ ] **Step 2: Создать GradientHuePicker.swift**

Содержимое `IWish/Views/Components/GradientHuePicker.swift`:

```swift
import SwiftUI

/// Радужный слайдер для выбора оттенка градиента обложки wishlist'а.
/// Превью градиента сверху анимирует hue в реальном времени.
struct GradientHuePicker: View {
    @Binding var hue: Double  // 0...1

    var body: some View {
        VStack(spacing: 16) {
            preview

            slider
                .frame(height: 32)
        }
    }

    @ViewBuilder
    private var preview: some View {
        let colors = DefaultCoverGenerator.colors(forHue: hue)
        ZStack {
            MeshGradient(
                width: 3, height: 3,
                points: [
                    .init(0, 0), .init(0.5, 0), .init(1, 0),
                    .init(0, 0.5), .init(0.5, 0.5), .init(1, 0.5),
                    .init(0, 1), .init(0.5, 1), .init(1, 1),
                ],
                colors: [
                    colors[0], colors[1], colors[2],
                    colors[1], colors[2], colors[0],
                    colors[2], colors[0], colors[1],
                ]
            )
        }
        .frame(height: 96)
        .clipShape(RoundedRectangle(cornerRadius: 14, style: .continuous))
        .animation(.easeOut(duration: 0.15), value: hue)
    }

    private var slider: some View {
        GeometryReader { geo in
            ZStack(alignment: .leading) {
                rainbow
                    .frame(height: 28)
                    .clipShape(Capsule())

                thumb
                    .offset(x: thumbOffsetX(in: geo.size.width))
                    .gesture(
                        DragGesture(minimumDistance: 0)
                            .onChanged { value in
                                let x = max(0, min(geo.size.width, value.location.x))
                                hue = x / geo.size.width
                            }
                    )
            }
            .frame(maxHeight: .infinity, alignment: .center)
        }
    }

    private var rainbow: some View {
        let stops = stride(from: 0.0, through: 1.0, by: 0.05).map { h in
            Gradient.Stop(color: Color(hue: h, saturation: 0.85, brightness: 0.92), location: h)
        }
        return LinearGradient(stops: stops, startPoint: .leading, endPoint: .trailing)
    }

    private var thumb: some View {
        Circle()
            .fill(Color(hue: hue, saturation: 0.85, brightness: 0.95))
            .overlay(Circle().strokeBorder(.white, lineWidth: 3))
            .shadow(color: .black.opacity(0.15), radius: 4, y: 2)
            .frame(width: 32, height: 32)
    }

    private func thumbOffsetX(in width: CGFloat) -> CGFloat {
        let x = width * hue - 16
        return max(0, min(width - 32, x))
    }
}

#Preview {
    @Previewable @State var hue: Double = 0.5
    return VStack {
        GradientHuePicker(hue: $hue)
            .padding()
        Text("hue = \(String(format: "%.2f", hue))")
    }
}
```

- [ ] **Step 3: Update DefaultCoverView — использовать colors(for:)**

В `IWish/Views/Components/DefaultCoverView.swift` (если файл существует — найти и проверить). Если рендерит градиент по `gradientSeed` — заменить на `DefaultCoverGenerator.colors(for: wishlist)`.

Если view принимает `id: UUID, imageData: Data?, emoji: String?, gradientSeed: Int? = nil` — добавить опциональный `gradientHue: Double? = nil`. И использовать:

```swift
let colors: [Color]
if let hue = gradientHue {
    colors = DefaultCoverGenerator.colors(forHue: hue)
} else if let seed = gradientSeed {
    colors = DefaultCoverGenerator.colors(forSeed: seed)
} else {
    colors = DefaultCoverGenerator.colors(for: id)
}
```

- [ ] **Step 4: Update WishlistTileView — pass hue в DefaultCoverView**

В `IWish/Views/Components/WishlistTileView.swift` найти место рендера фона тайла (`tileBackground`) и обновить вызов `DefaultCoverGenerator.colors(...)`:

```swift
let colors = DefaultCoverGenerator.colors(for: wishlist)
```

(вместо `colors(forSeed: wishlist.gradientSeed != 0 ? wishlist.gradientSeed : ...)`)

И аналогично в HomeView:wishlistTile если ещё используется (line 416 в HomeView):

```swift
let colors = DefaultCoverGenerator.colors(for: wishlist)
```

- [ ] **Step 5: AddWishlistSheet — integrate GradientHuePicker**

В `IWish/Views/AddWishlistSheet.swift`:

Добавить state:
```swift
@State private var gradientHue: Double = Double.random(in: 0...1)
```

В Form добавить новую секцию (только если нет фото):
```swift
if coverImageData == nil {
    Section("Цвет обложки") {
        GradientHuePicker(hue: $gradientHue)
            .padding(.vertical, 8)
    }
}
```

При save передавать в createWishlist. Если createWishlist в DataService требует обновления подписи — Task 5 уже это сделал, но мы могли пропустить gradientHue в createWishlist. Уточнить:

В `DataService.createWishlist`, добавить параметр:
```swift
func createWishlist(name: String, emoji: String?, coverImageData: Data? = nil, gradientHue: Double? = nil) async throws -> Wishlist {
    // ...
    let wishlist = Wishlist(
        name: name,
        coverImageData: coverImageData,
        coverEmoji: emoji,
        gradientSeed: 0,
        gradientHue: gradientHue
    )
    // ...
    try await firestore.createPersonalWishlist(
        uid: currentUID,
        wishlistID: wishlistID,
        name: name,
        emoji: emoji,
        gradientSeed: seed,
        gradientHue: gradientHue,
        coverImageData: coverImageData,
        key: key
    )
    // ...
}
```

И в AddWishlistSheet save вызывать с `gradientHue: coverImageData == nil ? gradientHue : nil` (если есть фото — не сохраняем hue).

- [ ] **Step 6: EditWishlistSheet — integrate GradientHuePicker**

Аналогично AddWishlistSheet. Initial state:
```swift
@State private var gradientHue: Double = wishlist.gradientHue ?? Double.random(in: 0...1)
```

В save вызывать `services.data.updateWishlist(...)` с `gradientHue: coverImageData == nil ? gradientHue : nil`.

- [ ] **Step 7: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -10
```

Expected: компилируется.

- [ ] **Step 8: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Views/Components/GradientHuePicker.swift IWish/Services/DefaultCoverGenerator.swift IWish/Views/Components/DefaultCoverView.swift IWish/Views/Components/WishlistTileView.swift IWish/Views/AddWishlistSheet.swift IWish/Views/EditWishlistSheet.swift IWish/Views/HomeView.swift IWish/Services/DataService.swift && git commit -m "ui: gradient hue picker for wishlist cover color"
```

---

## Task 17: HomeView — bottom safe area fix + iPad grid

**Files:**
- Modify: `IWish/Views/HomeView.swift`

- [ ] **Step 1: Bottom safe area fix**

Найти `var body: some View { ... }` (строка 47). Поменять расположение `.warmBackground()`:

```swift
var body: some View {
    Group {
        if hasAnyWishlists {
            wishlistList
        } else {
            emptyState
        }
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
    .warmBackground()
    .ignoresSafeArea(edges: .bottom)
    .navigationTitle("Вишлисты")
    .navigationBarTitleDisplayMode(.inline)
    // ... toolbar/sheets/onAppear как было
    .overlay(alignment: .bottom) {
        addButton
            .padding(.bottom, 24)
    }
}
```

И **удалить** `.warmBackground()` внутри `wishlistList` ScrollView (строка 326) — теперь warmBackground на корне.

- [ ] **Step 2: iPad adaptive grid**

Заменить hard-coded LazyVGrid columns (строка 293-296) на computed:

```swift
@Environment(\.horizontalSizeClass) private var sizeClass

private var gridColumns: [GridItem] {
    let count: Int
    switch sizeClass {
    case .compact: count = 2
    case .regular:
        count = (UIScreen.main.bounds.width > 1000) ? 4 : 3
    default: count = 2
    }
    return Array(repeating: GridItem(.flexible(), spacing: 12), count: count)
}
```

И в wishlistList:

```swift
LazyVGrid(columns: gridColumns, spacing: 12) {
    // ...
}
```

- [ ] **Step 3: Build verify**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -5
```

Expected: компилируется.

- [ ] **Step 4: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish/Views/HomeView.swift && git commit -m "home: bottom safe area fix + iPad adaptive grid columns"
```

---

## Task 18: iPad project settings — TARGETED_DEVICE_FAMILY + orientations

**Files:**
- Modify: `IWish.xcodeproj/project.pbxproj`

- [ ] **Step 1: Replace TARGETED_DEVICE_FAMILY**

В `IWish.xcodeproj/project.pbxproj`, найти все 4 строки `TARGETED_DEVICE_FAMILY = "1";` (Debug, Release, и ещё 2 для test target) и заменить на `TARGETED_DEVICE_FAMILY = "1,2";` (только для main IWish target — НЕ для test target, его оставить "1").

```bash
# Команда для main target:
sed -i '' 's/TARGETED_DEVICE_FAMILY = "1";/TARGETED_DEVICE_FAMILY = "1,2";/g' /Users/rugyron/Documents/Xcode/IWish/IWish.xcodeproj/project.pbxproj
```

ВНИМАНИЕ: эта команда заменит **все 4** вхождения. Если test target должен остаться iPhone-only — поправить вручную или использовать более точный sed pattern.

Альтернатива (точнее): открыть pbxproj в редакторе, найти XCBuildConfiguration блоки для IWish (не IWishTests), и менять только в них.

- [ ] **Step 2: Update INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad**

Найти `INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad = "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown";` и заменить на:

```
INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad = "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight";
```

```bash
sed -i '' 's|INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad = "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown";|INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad = "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight";|g' /Users/rugyron/Documents/Xcode/IWish/IWish.xcodeproj/project.pbxproj
```

iPhone оставить portrait-only (как было).

- [ ] **Step 3: Verify**

```bash
grep -E "TARGETED_DEVICE_FAMILY|UISupportedInterfaceOrientations_iPad" /Users/rugyron/Documents/Xcode/IWish/IWish.xcodeproj/project.pbxproj | head
```

Expected: TARGETED_DEVICE_FAMILY = "1,2" для main target, iPad orientations все 4.

- [ ] **Step 4: Build verify (можно сразу для iOS generic чтобы проверить иPad)**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'generic/platform=iOS' -configuration Debug -quiet OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -5
```

Expected: SUCCESS.

- [ ] **Step 5: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish.xcodeproj/project.pbxproj && git commit -m "project: enable iPad target + landscape orientations"
```

---

## Task 19: Tests — encryption roundtrip + price mode

**Files:**
- Create: `IWishTests/EncryptionRoundtripTests.swift`
- Create: `IWishTests/ItemPriceModeTests.swift`

- [ ] **Step 1: Создать EncryptionRoundtripTests**

Содержимое `IWishTests/EncryptionRoundtripTests.swift`:

```swift
import Testing
import CryptoKit
@testable import IWish

struct EncryptionRoundtripTests {
    @Test
    func roundtrip_with_all_v11_fields() throws {
        let key = EncryptionService.generateKey()
        let original: [String: Any] = [
            "name": "Mac Studio",
            "tier": "must",
            "price": 250000.0,
            "priceMax": 350000.0,
            "currency": "RUB",
            "url": "https://apple.com",
            "coverEmoji": "💻",
            "description": "Хочу самый топ M3 Ultra",
            "addedByUID": "uid123",
            "addedByName": "Влад"
        ]
        let encrypted = try EncryptionService.encrypt(original, using: key)
        let decrypted = try EncryptionService.decrypt(encrypted, using: key)

        #expect(decrypted["name"] as? String == "Mac Studio")
        #expect(decrypted["price"] as? Double == 250000.0)
        #expect(decrypted["priceMax"] as? Double == 350000.0)
        #expect(decrypted["description"] as? String == "Хочу самый топ M3 Ultra")
    }

    @Test
    func decrypt_payload_with_unknown_keys_succeeds() throws {
        // Forward-compat: будущая версия добавит новые ключи, наш клиент не должен падать.
        let key = EncryptionService.generateKey()
        let payload: [String: Any] = [
            "name": "Test",
            "tier": "maybe",
            "newFutureField_v1_2": "some-value",
            "another_unknown": 42
        ]
        let encrypted = try EncryptionService.encrypt(payload, using: key)
        let decrypted = try EncryptionService.decrypt(encrypted, using: key)
        // Codable пропускает unknown keys — все поля доступны
        #expect(decrypted["name"] as? String == "Test")
        #expect(decrypted["newFutureField_v1_2"] as? String == "some-value")
    }

    @Test
    func data_field_packed_unpacked_roundtrip() throws {
        let key = EncryptionService.generateKey()
        let imageBytes = Data((0..<200).map { _ in UInt8.random(in: 0...255) })

        let payload = EncryptionService.packPayload([
            "name": "Photo item",
            "coverImageData": imageBytes
        ])
        let encrypted = try EncryptionService.encrypt(payload, using: key)
        let decrypted = try EncryptionService.decrypt(encrypted, using: key)

        let restoredData = EncryptionService.dataField(decrypted, "coverImageData")
        #expect(restoredData == imageBytes)
    }
}
```

- [ ] **Step 2: Создать ItemPriceModeTests**

Содержимое `IWishTests/ItemPriceModeTests.swift`:

```swift
import Testing
@testable import IWish

struct ItemPriceModeTests {
    @Test
    func priceMode_none_when_both_nil() {
        let item = Item(name: "X", price: nil, priceMax: nil)
        if case .none = item.priceMode {
            // OK
        } else {
            Issue.record("Expected .none")
        }
    }

    @Test
    func priceMode_exact_when_only_price() {
        let item = Item(name: "X", price: 1000, priceMax: nil)
        if case .exact(let p) = item.priceMode {
            #expect(p == 1000)
        } else {
            Issue.record("Expected .exact")
        }
    }

    @Test
    func priceMode_range_when_both_present() {
        let item = Item(name: "X", price: 500, priceMax: 1500)
        if case .range(let min, let max) = item.priceMode {
            #expect(min == 500)
            #expect(max == 1500)
        } else {
            Issue.record("Expected .range")
        }
    }

    @Test
    func priceMode_swaps_when_min_greater_than_max() {
        let item = Item(name: "X", price: 1500, priceMax: 500)
        if case .range(let min, let max) = item.priceMode {
            #expect(min == 500)
            #expect(max == 1500)
        } else {
            Issue.record("Expected .range with swapped")
        }
    }
}
```

- [ ] **Step 3: Run tests**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild test -scheme IWish -destination 'platform=iOS Simulator,name=iPhone 16 Pro' OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" 2>&1 | tail -30
```

Expected: PASS для всех.

- [ ] **Step 4: Commit**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWishTests/EncryptionRoundtripTests.swift IWishTests/ItemPriceModeTests.swift && git commit -m "tests: encryption roundtrip + price mode invariants"
```

---

## Task 20: Manual integration test на iPhone Влада

**Files:** none (just verify behavior)

- [ ] **Step 1: Build + install + launch на iPhone**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild build -scheme IWish -destination 'platform=iOS,id=17A9B107-CE43-5472-9087-F1C57E8F1E58' -configuration Debug OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" -quiet
```

Expected: SUCCESS. Найти .app:

```bash
APP_PATH=$(find /Users/rugyron/Documents/Xcode/IWish/build -name "IWish.app" -type d 2>/dev/null | head -1)
xcrun devicectl device install app --device 17A9B107-CE43-5472-9087-F1C57E8F1E58 "$APP_PATH"
xcrun devicectl device process launch --device 17A9B107-CE43-5472-9087-F1C57E8F1E58 --terminate-existing RUGyron.IWish
```

- [ ] **Step 2: Manual checklist**

Влад проходит на iPhone:
- [ ] Запуск → splash → HomeView без чёрного снизу
- [ ] Settings → имя нормальное (не "Пользователь"), версия "1.1" без билда
- [ ] Existing wishlist → существующие descriptions сохранились (backfill)
- [ ] Новый item с описанием → описание видно в EditItemSheet после редактирования
- [ ] Создать новый item с диапазоном цены → карточка показывает "до Y", детальный экран "X — Y"
- [ ] EditItem → priceField без ".0"
- [ ] Дроп лист "изменить важность" → эмодзи на всех опциях, checkmark рядом с выбранной
- [ ] Tap по фону формы → клавиатура закрывается. Свайп вниз по списку → закрывается.
- [ ] Открыть EditWishlistSheet (без фото) → виден GradientHuePicker. Сменить hue → перезайти → сохранилось.
- [ ] Карточки желаний с corner radius 8 (визуально менее скруглённые)

- [ ] **Step 3: Manual checklist на iPad (если есть)**

Влад на iPad:
- [ ] Установить app
- [ ] Запуск в portrait → 3 колонки grid
- [ ] Поворот landscape → 4 колонки + master/detail (HomeView слева, выбранный wishlist справа)
- [ ] Двойной тап top bar → scroll-to-top
- [ ] Свайп снизу → Dock появляется

- [ ] **Step 4: Verify Firestore через Firebase Console**

Влад:
- [ ] Открыть Firebase Console → проект rewardpierwebpush → Firestore
- [ ] Найти wishlist с item у которого было описание → проверить encryptedPayload изменился (есть нечитабельные байты — это нормально, шифрование). Но размер `encryptedPayload` должен вырасти после backfill.
- [ ] memberships document для текущего юзера → видно поле `userName` со значением

---

## Task 21: Force Update тест

**Files:** none (config через Firebase Console)

- [ ] **Step 1: Setup Remote Config в Firebase Console**

Влад:
1. Firebase Console → Build → Remote Config → Create Configuration
2. Add parameter: `min_supported_build` (Number), default 0
3. Add parameter: `force_update_message` (String), default ""
4. Publish (Save changes button)

- [ ] **Step 2: Test force update flow**

Влад:
1. В Firebase Console установить `min_supported_build = 99` (заведомо больше текущего билда). Publish.
2. На iPhone убить app, запустить заново.
3. Должен появиться `ForceUpdateBlockingView` с кнопкой "Открыть App Store".
4. Сбросить в Firebase Console: `min_supported_build = 0`. Publish.
5. Перезапустить app → главный экран.

Expected: blocking screen появляется, после reset — нормальный flow.

---

## Task 22: Version bump + ASC submit

**Files:**
- Modify: `IWish.xcodeproj/project.pbxproj` (CFBundleShortVersionString + CFBundleVersion)

- [ ] **Step 1: Bump versions**

```bash
sed -i '' 's/MARKETING_VERSION = 1.0;/MARKETING_VERSION = 1.1;/g' /Users/rugyron/Documents/Xcode/IWish/IWish.xcodeproj/project.pbxproj
sed -i '' 's/CURRENT_PROJECT_VERSION = 4;/CURRENT_PROJECT_VERSION = 5;/g' /Users/rugyron/Documents/Xcode/IWish/IWish.xcodeproj/project.pbxproj
```

Verify:
```bash
grep -E "MARKETING_VERSION|CURRENT_PROJECT_VERSION" /Users/rugyron/Documents/Xcode/IWish/IWish.xcodeproj/project.pbxproj
```

Expected: MARKETING_VERSION = 1.1, CURRENT_PROJECT_VERSION = 5.

- [ ] **Step 2: Archive в Release**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild archive \
    -scheme IWish \
    -destination 'generic/platform=iOS' \
    -configuration Release \
    -archivePath ./build/IWish-build5.xcarchive \
    OTHER_CODE_SIGN_FLAGS="--strip-disallowed-xattrs" \
    -quiet
```

Expected: SUCCESS, archive created.

- [ ] **Step 3: Export + upload через ASC API**

Создать `ExportOptions.plist` если ещё нет (взять из предыдущего билда).

```bash
cd /Users/rugyron/Documents/Xcode/IWish && xcodebuild -exportArchive \
    -archivePath ./build/IWish-build5.xcarchive \
    -exportOptionsPlist ./build/ExportOptions.plist \
    -exportPath ./build/build5-export \
    -authenticationKeyPath ~/.config/iwish-asc-api.p8 \
    -authenticationKeyID 732G3WGLG7 \
    -authenticationKeyIssuerID 79b933f4-... \
    -allowProvisioningUpdates
```

(точные authenticationKey ID/Issuer возьми из ASC reference в vault)

Expected: upload to ASC succeeded.

- [ ] **Step 4: Создать App Store Version 1.1 + прицепить build #5**

Через ASC API или UI:
1. Создать новую версию 1.1.
2. Дождаться processing build #5 (~15-30 мин).
3. Прицепить build #5 к версии 1.1.

- [ ] **Step 5: Заполнить What's New (UI)**

Через ASC web UI (API не поддерживает):

```
• Полная адаптация под iPad
• Возможность задать диапазон цены желания
• Пикер цвета обложки списка
• Описания желаний теперь синхронизируются между устройствами
• Множество улучшений: имя пользователя, клавиатура, отображение цен
```

- [ ] **Step 6: Submit to review**

Через UI или API. Дождаться WAITING_FOR_REVIEW.

- [ ] **Step 7: Commit version bump**

```bash
cd /Users/rugyron/Documents/Xcode/IWish && git add IWish.xcodeproj/project.pbxproj && git commit -m "version: bump to 1.1 build 5" && git push
```

- [ ] **Step 8: Setup `min_supported_build = 5` в Remote Config (после апрува)**

После того как Apple апрувнет v1.1 и юзеры обновятся:
- В Firebase Console → Remote Config → `min_supported_build = 5`. Publish.
- Это заблокирует v1.0 (build 4) для новых запусков.

---

## Self-review checklist (выполнить перед началом execution)

- [ ] Все 16 пунктов спеки имеют свой Task
- [ ] Migration safety (раздел 1 спеки) — Tasks 2, 3, 5 (preserve unknown keys + backfill)
- [ ] Force Update (#16) — Tasks 8, 9, 21
- [ ] iPad (#15) — Tasks 9 (RootView split), 17 (HomeView grid), 18 (project settings)
- [ ] Имя как gate (#2) — Tasks 6, 7, 9 (RootView gate)
- [ ] Member names (#5/#12) — Tasks 4, 5 (backfill memberships userName), 11
- [ ] Description sync (#14) и другие missing fields — Tasks 2, 3, 5
- [ ] Range pricing (#8) — Tasks 1, 5, 14, 15
- [ ] Gradient hue (#7) — Task 16
- [ ] Card radius (#10) — Task 14
- [ ] Tier emoji (#6) — Task 14
- [ ] Bottom safe area (#9) — Task 17
- [ ] Decimal display (#4) — Task 13
- [ ] Keyboard (#3) — Task 13
- [ ] Version caption (#11) — Task 12
- [ ] Rate limiter (#1) — Task 4

---

**Plan complete.**
